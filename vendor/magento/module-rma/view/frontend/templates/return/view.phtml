<?php
/**
 * Copyright © Magento, Inc. All rights reserved.
 * See COPYING.txt for license details.
 */

// @codingStandardsIgnoreFile

/** @var $block \Magento\Rma\Block\Returns\View */
?>

<?php
    $_rma = $block->getRma();
    $_order = $block->getOrder();
    $additionalData = $block->getAdditionalData();
    $rmaHelper = $block->getRmaHelper();
?>

<div class="block block-return-details">
    <div class="block-content">
        <div class="box box-returns-requested">
            <strong class="box-title"><span><?= /* @escapeNotVerified */ __('Request Information') ?></span></strong>
            <div class="box-content">
                <?= /* @escapeNotVerified */ __('ID') ?>: <?= $block->escapeHtml($_rma->getIncrementId()) ?><br>
                <?= /* @escapeNotVerified */ __('Order ID') ?>: <a href="<?= /* @escapeNotVerified */ $block->getOrderUrl($_rma) ?>"><?= $block->escapeHtml($_rma->getOrderIncrementId()) ?></a><br>
                <?= /* @escapeNotVerified */ __('Date Requested') ?>: <?= /* @escapeNotVerified */ $rmaHelper->getFormatedDate($_rma->getDateRequested()) ?><br>
                <?= /* @escapeNotVerified */ __('Email') ?>: <?= $block->escapeHtml($_order->getCustomerEmail()) ?>
                <?php if ($_rma->getCustomerCustomEmail()): ?>
                    <br>
                    <?= /* @escapeNotVerified */ __('Contact Email Address') ?>: <?= $block->escapeHtml($_rma->getCustomerCustomEmail()) ?>
                <?php endif ?>
            </div>
        </div>
        <div class="box box-treturns-shipping">
            <strong class="box-title"><span><?= /* @escapeNotVerified */ __('Shipping Address') ?></span></strong>
            <div class="box-content">
                <address>
                    <?= /* @escapeNotVerified */ $block->getAddress() ?>
                </address>
            </div>
        </div>
    </div>
</div>

<div class="returns-details-items">
    <span id="rma-please-wait" class="please-wait load indicator" style="display: none;" data-text="<?= /* @escapeNotVerified */ __('This won\'t take long . . .') ?>">
        <span><?= /* @escapeNotVerified */ __('This won\'t take long . . .') ?></span>
    </span>

    <?php $trackingNumbers = $block->getTrackingNumbers() ?>
    <?php if ($block->isPrintShippingLabelAllowed() || $trackingNumbers->count()): ?>
        <div class="block block-returns-tracking">
            <div class="block-title">
                <strong><?= /* @escapeNotVerified */ __('Shipping and Tracking Information') ?></strong>
                <?php if ($trackingNumbers->count() || $block->canShowButtons()): ?>
                <div class="actions-track">
                    <?php if ($trackingNumbers->count()): ?>
                        <a class="action track" href="#" data-mage-init='{"popupWindow": {
                            "windowURL":"<?= $block->escapeUrl($rmaHelper->getTrackingPopupUrlBySalesModel($block->getRma())) ?>",
                            "windowName":"trackshipment",
                            "width":800,"height":600,"top":0,"left":0,"resizable":1,"scrollbars":1
                        }}' title="<?= $block->escapeHtml(__('Track this shipment')) ?>">
                            <span><?= $block->escapeHtml(__('Track this shipment')) ?></span>
                        </a>
                    <?php endif ?>
                    <?php if ($block->canShowButtons()): ?>
                        <a href="<?= $block->escapeUrl($rmaHelper->getPackagePopupUrlByRmaModel($block->getRma(), 'printlabel')) ?>"
                           title="<?= $block->escapeHtml(__('Print Shipping Label')) ?>"
                           class="action track">
                            <span><?= $block->escapeHtml(__('Print Shipping Label')) ?></span>
                        </a>
                        <a class="action track" href="#" data-mage-init='{"popupWindow": {
                            "windowURL":"<?= $block->escapeUrl($rmaHelper->getPackagePopupUrlByRmaModel($block->getRma())) ?>",
                            "windowName":"package",
                            "width":800,"height":600,"top":0,"left":0,"resizable":1,"scrollbars":1
                        }}' title="<?= $block->escapeHtml(__('Show Packages')) ?>">
                            <span><?= $block->escapeHtml(__('Show Packages')) ?></span>
                        </a>
                    <?php endif; ?>
                </div>
                <?php endif; ?>
            </div>
            <div class="block-content">
                <form method="post"
                      id="rma_track_info"
                      class="form form-tracking"
                      data-mage-init='{"rmaTrackInfo":{"addLabelUrl": "<?= /* @escapeNotVerified */ $block->getAddLabelUrl() ?>"}, "validation":{}}'>
                    <div class="table-wrapper">
                        <table class="data table" id="track-info-table">
                            <caption class="table-caption"><?= /* @escapeNotVerified */ __('Shipping and Tracking Information') ?></caption>
                            <thead>
                                <tr>
                                    <th scope="col" class="col carrier"><?= /* @escapeNotVerified */ __('Carrier') ?></th>
                                    <th scope="col" class="col id"><?= /* @escapeNotVerified */ __('Number') ?></th>
                                    <th scope="col" class="col actions"><?= /* @escapeNotVerified */ __('Action') ?></th>
                                </tr>
                            </thead>
                            <tbody id="track-info-tbody">
                                <?= /* @escapeNotVerified */ $block->getTrackingInfo('tracking') ?>
                            </tbody>
                            <?php if ($block->isPrintShippingLabelAllowed()): ?>
                            <tfoot>
                                <tr>
                                    <td class="col carrier">
                                        <select name="tracking_carrier_select" id="tracking_carrier_select" class="select" data-validate="{required:true}">
                                            <option value=""></option>
                                            <?php foreach ($block->getCarriers() as $key => $value): ?>
                                                <option value="<?= /* @escapeNotVerified */ $key ?>"><?= $block->escapeHtml($value) ?></option>
                                            <?php endforeach ?>
                                        </select>
                                    </td>
                                    <td class="col id">
                                        <input type="text" class="input-text" value="" name="tracking_number_input" id="tracking_number_input" data-validate="{required:true}">
                                    </td>
                                    <td class="col actions">
                                        <button type="button" class="action add" id="btn-add-track-number" title="<?= /* @escapeNotVerified */ __('Add') ?>"><span><?= /* @escapeNotVerified */ __('Add') ?></span></button>
                                    </td>
                                </tr>
                            </tfoot>
                            <?php endif ?>
                        </table>
                    </div>
                    <input name="form_key" type="hidden" value="<?= /* @noEscape */ $block->getFormKey() ?>" />
                </form>
            </div>
        </div>
    <?php endif ?>

    <div class="block block-returns-items">
        <div class="block-title">
            <strong><?= /* @escapeNotVerified */ __('Items Return Requested For') ?></strong>
        </div>
        <div class="block-content">
            <div class="table-wrapper table-returns">
                <table class="data table table-returns" id="my-returns-items-table">
                    <caption class="table-caption"><?= /* @escapeNotVerified */ __('Items Return Requested For') ?></caption>
                    <thead>
                        <tr>
                            <?php $availableAttributes = $block->getVisibleAttributes(); ?>
                            <th scope="col" class="col id"><?= /* @escapeNotVerified */ __('Product Name') ?></th>
                            <th scope="col" class="col sku"><?= /* @escapeNotVerified */ __('SKU') ?></th>
                            <?php if (isset($availableAttributes['condition'])): ?>
                                <th scope="col" class="col condition"><?= /* @escapeNotVerified */ __('Condition') ?></th>
                            <?php endif; ?>
                            <?php if (isset($availableAttributes['condition'])): ?>
                                <th scope="col" class="col resolution"><?= /* @escapeNotVerified */ __('Resolution') ?></th>
                            <?php endif; ?>
                            <th scope="col" class="col request qty"><?= /* @escapeNotVerified */ __('Request Qty') ?></th>
                            <th scope="col" class="col qty"><?= /* @escapeNotVerified */ __('Qty') ?></th>
                            <th scope="col" class="col status"><?= /* @escapeNotVerified */ __('Status') ?></th>
                        </tr>
                    </thead>
                    <tbody>
                        <?php $_qna = null; ?>
                        <?php foreach ($block->getItems() as $key => $item): ?>
                            <?php foreach ($block->getRealValueAttributes($item->getId()) as $code => $attribute) {
    if (!in_array($code, $block->getAttributeFilter()) && !empty($attribute['value']) && $block->getAttributeLabel($item->getId(), $code)
        && false !== $block->getAttributeValue($item->getId(), $code)) {
        $_qna .= '
                                        <tr><td class="col questions">' . $block->getAttributeLabel($item->getId(), $code) . '</td>
                                        <td class="col answers">' . $block->getAttributeValue($item->getId(), $code) . '</td></tr>
                                    ';
    }
} ?>
                        <tr>
                            <td data-th="<?= $block->escapeHtml(__('Product Name')) ?>" class="col id">
                                <?= $block->escapeHtml($item->getProductName()) ?>
                                <?php if ($_options = $block->getItemOptions($item)): ?>
                                <dl class="item options">
                                <?php foreach ($_options as $_option) : ?>
                                    <dt><?= $block->escapeHtml($_option['label']) ?></dt>
                                    <dd><?= /* @escapeNotVerified */ $_option['value'] ?></dd>
                                <?php endforeach; ?>
                                </dl>
                                <?php endif; ?>
                                <?php if ($_qna) : ?>
                                    <button class="action details" type="button" data-mage-init='{"toggleAdvanced": {"newLabel": "<?= /* @escapeNotVerified */ __('Hide Details') ?>", "toggleContainers": "#shf_<?= /* @escapeNotVerified */ $key ?>"}}'>
                                        <span><?= /* @escapeNotVerified */ __('Show Details') ?></span>
                                    </button>
                                <?php endif; ?>
                            </td>
                            <td data-th="<?= $block->escapeHtml(__('SKU')) ?>" class="col sku"><?= $block->escapeHtml($item->getProductSku()) ?></span></td>
                            <?php if (false !== $block->getAttributeValue($item->getId(), 'condition')) : ?>
                                <td data-th="<?= $block->escapeHtml(__('Condition')) ?>" class="col condition"><?= $block->escapeHtml($block->getAttributeValue($item->getId(), 'condition')) ?></td>
                            <?php endif; ?>
                            <?php if (false !== $block->getAttributeValue($item->getId(), 'condition')) : ?>
                                <td data-th="<?= $block->escapeHtml(__('Resolution')) ?>" class="col resolution"><?= $block->escapeHtml($block->getAttributeValue($item->getId(), 'resolution')) ?></td>
                            <?php endif; ?>
                            <td data-th="<?= $block->escapeHtml(__('Request Qty')) ?>" class="col request qty"><?= $block->escapeHtml($rmaHelper->parseQuantity($item->getQtyRequested(), $item)) ?></td>
                            <td data-th="<?= $block->escapeHtml(__('Qty')) ?>" class="col qty"><?= $block->escapeHtml($rmaHelper->getQty($item)) ?></td>
                            <td data-th="<?= $block->escapeHtml(__('Status')) ?>" class="col status"><?= $block->escapeHtml($item->getStatusLabel()) ?></td>
                        </tr>
                        <?php if ($_qna) : ?>
                            <tr id="shf_<?= /* @escapeNotVerified */ $key ?>" class="hidden">
                                <td colspan="7">
                                    <table class="data table">
                                        <caption class="table-caption"><?= /* @escapeNotVerified */ __('Questions and Answers') ?></caption>
                                        <thead>
                                            <tr>
                                                <th class="col questions"><?= /* @escapeNotVerified */ __('Questions') ?></th>
                                                <th class="col answers"><?= /* @escapeNotVerified */ __('Answers') ?></th>
                                            </tr>
                                        </thead>
                                        <?= /* @escapeNotVerified */ $_qna ?>
                                    </table>
                                </td>
                            </tr>
                        <?php endif; ?>
                        <?php endforeach; ?>
                    </tbody>
                </table>
            </div>
        </div>
    </div>

    <div class="block block-returns-comments">
        <div class="block-title"><strong><?= /* @escapeNotVerified */ __('Comments') ?></strong></div>
        <div class="block-content">
            <dl class="returns-comments">
                <?php $_comments = $block->getComments(); ?>
                <?php if (!empty($_comments)): ?>
                    <?php foreach ($_comments as $_comment): ?>
                        <?php if ($_comment->getIsVisibleOnFront()): ?>
                            <dt class="returns-comment-date">
                                <strong class="returns-comment-autor">
                                    <?php if ($_comment->getIsAdmin()): ?>
                                        <?= /* @escapeNotVerified */ __('Customer Service') ?>
                                    <?php else: ?>
                                        <?= $block->escapeHtml($block->getCustomerName()) ?>
                                    <?php endif; ?>
                                </strong>
                                <?= /* @escapeNotVerified */ $block->formatDate($_comment->getCreatedAt()) ?> <?= /* @escapeNotVerified */ $block->formatTime($_comment->getCreatedAt()) ?>
                            </dt>
                            <dd class="returns-comment-text">
                                <?= nl2br($block->escapeHtml($_comment->getComment())) ?>
                            </dd>
                        <?php endif; ?>
                    <?php endforeach; ?>
                <?php endif; ?>
            </dl>
            <form method="post"
                  id="rma_comment"
                  action="<?= /* @escapeNotVerified */ $block->getSubmitUrl() ?>"
                  class="form form-returns-comments"
                  data-mage-init='{"validation":{}}'>
                <fieldset class="fieldset">
                    <div class="field comment">
                        <label class="label"><span><?= /* @escapeNotVerified */ __('Leave Comment') ?></span></label>
                        <div class="control">
                            <textarea id="rma_comment_text" cols="5" rows="3" name="comment" data-validate="{required:true}"></textarea>
                        </div>
                    </div>
                </fieldset>
                <div class="actions-toolbar">
                    <div class="primary">
                        <button type="submit" class="action submit primary" id="submit.save"><span><?= /* @escapeNotVerified */ __('Submit Comment') ?></span></button>
                    </div>
                </div>
                <input id="label-formkey"name="form_key" type="hidden" value="<?= /* @noEscape */ $block->getFormKey() ?>" />
            </form>
        </div>
    </div>
</div>
